import 'package:flutter/material.dart';

Widget EMPTY_WIDGET = const SizedBox.shrink();

const double VERY_SMALL_SPACE = 4.0;
const double SMALL_SPACE = 8.0;
const double MEDIUM_SPACE = 12.0;
const double LARGE_SPACE = 16.0;
const double VERY_LARGE_SPACE = 20.0;

const double VERY_SMALL_FONT_SIZE = 12.0;
const double SMALL_FONT_SIZE = 14.0;
const double MEDIUM_FONT_SIZE = 16.0;
const double LARGE_FONT_SIZE = 18.0;
const double VERY_LARGE_FONT_SIZE = 20.0;

const BorderRadius BORDER_RADIUS_SMALL =
    BorderRadius.all(Radius.circular(SMALL_SPACE));

const BorderRadius BORDER_RADIUS_MEDIUM =
    BorderRadius.all(Radius.circular(MEDIUM_SPACE));

const BorderRadius BORDER_RADIUS_LARGE =
    BorderRadius.all(Radius.circular(LARGE_SPACE));
